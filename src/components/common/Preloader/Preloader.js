import preLoader from "../../../assets/images/loader.svg";
import React from "react";

let PreLoader = (props) => {
    return <div>
      <img src={preLoader}/>
    </div>
}

export default PreLoader;